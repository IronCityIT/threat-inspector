{
  "name": "threat-inspector-trigger-function",
  "description": "Iron City Threat Inspector triggerScan function (scaffold — deployed by Bill)",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "echo \"no lint configured\" && exit 0",
    "deploy": "echo \"Deploy is performed by Bill, not the agent.\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
