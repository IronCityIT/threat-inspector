# OPTIONAL local-model remediation. Not needed to scan, ingest, or report.
#
#     pip install -r requirements.txt -r requirements-ai.txt
#
# `utils/remediation.py` imports `ollama` lazily, inside try/except, and falls
# back to curated guidance when it is absent — so leaving this file uninstalled
# degrades one feature rather than breaking anything. The scan pipeline never
# touches it, and the default engine is "static", which uses no model at all.
#
# What used to be here and is not any more: transformers, torch, nltk and
# scikit-learn. Exactly one thing imported any of them — a remediation path that
# generated security advice with gpt2 — and that path is gone. torch alone is a
# 527 MB wheel dragging in ~1.2 GB of CUDA libraries, which is a large
# supply-chain surface for a security product to carry for a feature that no
# longer exists.
#
# Note for review: per CLAUDE.md, AI analysis is the consensus-engine's job and
# must not be duplicated in a product. The Ollama path below predates that rule
# and arguably should go too — flagged in docs/HANDOFF.md, not decided here. It
# is at least opt-in now, and anything it produces is labelled as generated
# before it reaches a client.
-r requirements.txt

ollama>=0.1.0
