{
  "name": "threat-inspector-functions",
  "description": "Iron City Threat Inspector Cloud Functions (storeScanResults ingest)",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "echo \"no lint configured\" && exit 0",
    "serve": "firebase emulators:start --only functions",
    "deploy": "echo \"Deploy is performed by Bill, not the agent.\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
